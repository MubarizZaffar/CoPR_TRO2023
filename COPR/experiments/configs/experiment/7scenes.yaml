# @package _group_
experiment_params:
  name: 7scenes
  bs: 16
  n_workers: 8
  paths:
    img_path: ${paths.datasets_home_dir}/7scenes
    test_pairs_fname: ${paths.work_dir}/assets/data/NN_7scenes.txt
  output:
    home_dir: ${paths.output_home_dir}/${model.model_params.name}/${experiment.experiment_params.name}
    res_txt_fname: ${experiment.experiment_params.output.home_dir}/est_rel_poses.txt

data_params:
  work_dir: /home/mzaffar/Documents/codes/COPR
  img_dir: /home/mzaffar/Documents/datasets/7scenes
  train_pairs_fname: ${data_params.work_dir}/assets/data/db_all_med_hard_train.txt
  val_pairs_fname: ${data_params.work_dir}/assets/data/db_all_med_hard_valid.txt
